import { Request, Response } from "express";
import { asyncHandler } from "express-strategy";



export class Problem{
    create = asyncHandler(async (req: Request, res: Response) => {
        
    })
}